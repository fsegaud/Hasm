alias $eeprom_i d0.0
alias $eeprom_v d0.1
alias $eeprom_l d0.2

alias $len r0
rd $len $eeprom_l

# fill the eeprom with 0-9 values
alias $i r1
mov $i 0
loop:
    wd $eeprom_i $i
    wd $eeprom_v $i
    inc $i
    blt loop $i $len
    
wd $eeprom_i 31
rd r2 $eeprom_v
assert r2 31

ret
